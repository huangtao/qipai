# need stl
#APP_STL := gnustl_static

# enable c++11 and exception
#APP_CPPFLAGS := -std=c++11 -fsigned-char
#APP_CPPFLAGS += -fexceptions

# the default is armeabi
# for all cpu
#APP_ABI := all

# minimal android version is 3.1.x
APP_PLATFORM := android-12
